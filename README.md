## To run tests you must install the following gems

`gem install rspec`
`gem install rspec-expectations`

## To initialize rspec

`rspec --init`

# it creates files
# .rspec
# spec/spec_helper.rb`

## To run tests:
`rspec spec`
